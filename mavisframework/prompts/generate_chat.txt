The following is a brief description of ${agent}:
${base_desc}

The following is ${agent}'s memory:
${memory}

Current location: ${address}
Current time: ${current_time}

${relation}
${previous_context}${current_context}
${agent} is starting a conversation with ${another}. The following is their conversation log:
<Conversation Log>
${conversation}
</Conversation Log>

<Conversation Guidelines>
- ${agent} must not repeat content that already appears in <Conversation Log>
- The conversation must be consistent with the agent's personality and current situation
- Use natural, fluent language that fits everyday communication
- Keep responses within 1-3 sentences
- Output only ${agent}'s dialogue content directly, without any additional information
</Conversation Guidelines>

Based on the <Conversation Log> and <Conversation Guidelines> above, now ${agent} will say to ${another}:
