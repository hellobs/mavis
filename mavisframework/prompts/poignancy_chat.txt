${base_desc}

Rate on a scale of 1 to 10, using the following criteria:
1 means extremely ordinary, such as a routine morning greeting;
10 means extremely special or intense and memorable, such as a conversation about a breakup or an argument.
Each conversation must be rated with an integer from 1 to 10 only. For example:
Conversation: a routine morning greeting. Rating: 1
Conversation: a conversation about a breakup or an argument. Rating: 10

Below is the complete conversation that ${agent} needs to rate:
"""
${event}
"""
Rating: <score>

Fill in <score> based on the complete event.
Format requirements: output only a single number in the range of 1 to 10, and nothing other than that number.
