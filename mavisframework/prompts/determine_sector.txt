Choose an appropriate sector for the current task from the sector options.

${agent} lives in <${live_sector}>, which contains ${live_arenas}.
${agent}'s current location is <${current_sector}>, which contains ${current_arenas}.
${daily_plan}
Question:
${agent} is currently ${complete_plan}. To ${decomposed_plan}, where should ${agent} go?

Requirements:
1. You must choose a sector from this list: [${areas}].
2. If currently located in one of the listed sectors and the planned activity can be carried out there, prefer to stay in the current sector.
3. Do not choose a sector outside the list.
4. Output the selected sector directly.

${agent} should go to:
