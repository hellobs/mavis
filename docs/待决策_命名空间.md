# 待决策:导入命名空间(Mavis 重名问题)

> 记录日期:2026-08-22。此事项暂缓,避免遗忘。

## 现状

- 框架内部用 `from framework.xxx` 导入(42 处,17 个文件),全部是内部模块互相引用,无外部依赖
- 问题:对外导入路径 `framework.xxx` 偏泛、无辨识度,且有撞名风险

## 想法(当时讨论的方案)

- **方案 B(推荐)**:保留 `framework/` 目录,新增 `mavis/` 包(只放 `__init__.py`,转发到 framework 子模块)——对外 `from mavis.xxx import`,内部不动
- **方案 A**:目录全改名 `framework/ → mavis/`,42 处导入全改

## 暂缓原因

- **市面上已有 Mavis 同名项目**,需先确认:
  - 包名冲突(pip 上是否有 mavis)
  - 品牌混淆(评委/用户可能混淆)
  - 是否需要替代命名(如 mavis-framework / mavis-ga / 其他)

## 后续行动

1. 调研现有 Mavis 项目的领域和包名,评估冲突程度
2. 确定替代命名或确认可用
3. 选定方案 B(轻)或 A(彻底)执行
4. 执行后同步:README、docs、config_tool 的 import

---
*本文件独立存放,待命名决策后更新或归档。*
