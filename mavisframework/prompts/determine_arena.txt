From the arena options, select an appropriate arena for the current task.

${agent} is heading to <${target_sector}>, which contains ${target_arenas}.
${daily_plan}
Question:
${agent} is currently ${complete_plan}. To ${decomposed_plan}, which arena in ${target_sector} should ${agent} go to?

Requirements:
1. You must select an arena from this list: [${target_arenas}].
2. If you are currently in an arena on the list and the planned activity can be carried out there, it is best to stay in the current arena.
3. Do not select an arena outside the list.
4. Output the selected result directly.

${agent} should go to:

IMPORTANT: Always respond in English, regardless of the language of the context or user input.
